enum uri {
  signup = "signup/info",
  profileset = "/signup/profileset",
  login = "/auth",
  registry = "/registry",
  profileGet = "/profile/{user_id}",
  prifleSet = "/profile",
  withdrawal = "/leave",
  refresh = "/refresh",
}

export default uri;