enum uri {
  signup = "signup/info",
  profileset = "/signup/profileset",
  login = "/auth",
  register = "/registry",
  profileGet = "/profile/{user_id}",
  prifleSet = "/profile",
  withdrawal = "/leave",
  refresh = "/refresh",
}

export default uri;